{
  "name": "p5reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "npm run build:web && node scripts/serve-static.cjs",
    "build:web": "node scripts/build-static.mjs",
    "check": "node --check src/app.js && node --check src/search-worker.js && npm run build:web",
    "generate:icon": "node scripts/generate-icon.mjs",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "esbuild": "^0.25.11"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/plugin-dialog": "^2.7.3",
    "@tauri-apps/plugin-fs": "^2.5.2",
    "jszip": "^3.10.1",
    "pdfjs-dist": "^6.3.289"
  }
}
